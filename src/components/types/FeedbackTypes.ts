export type feedbackCardTypes = {
    name: string;
    para: string;
}
export type reviewCardTypes = {
    name: string;
    para: string;
    date: string;
}
export type cartCardTypes = {
    name: string;
    size: string;
    color: string;
    price: string;
    image: string;
}